# Subtasks of *[Task Name]*

| ID | State | Summary |
| -- | ----- | ------- |

[ID is the subtask directory name, e.g., "001-add-login-endpoint"]
[State is "APPROVED", "REJECTED", or "PENDING" from the subtask state file; if the file is missing or empty, use "PENDING"]
[Summary is a concise summary extracted from the subtask commit_message file]
